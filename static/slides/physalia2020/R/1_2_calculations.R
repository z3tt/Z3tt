########################################################################
# Physalia Course "Introduction to DataVisualization in R with ggplot2 #
#   Cédric Scherer (cedricphilippscherer@gmail.com) | March 2-6 2020   #
########################################################################

#### BASIC CALCULATIONS + ASSIGNMENTS ##################################
#-----------------------------------------------------------------------

#!! Type `2 + 3` and hit run.

#!! Play around with other operators, e.g. `-`, `*`, `/`, `^`, `sqrt`.
#?? What is the difference between the first 5 and `sqrt`?

## Examples:
2536578 + 3245
(59 + 73 + 2) * 5
1 / 200 * 30
sin(pi / 2)
10^12 * sqrt(4312)
log(exp(5))

## EXERCISE 1 ########################################################
#!! Next, run `x <- 1` and and afterwards `x` and `x + 2`.
#?? Why is there no output when you run the first command?
#?? What is the arrow symbol `<-` doing? 
#?? (Tipp: Have look at the environment pane)

#!! Now run `x <- 5` and `x + 2`.
#?? What is the value of x now?

#?? What if you type `y <- x` and what if `y <- x <- 2`
#?? And what happens if you type `y <- 2 <- x`? 
